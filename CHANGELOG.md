# Changelog

## 1.5.0
- Add port-forwarding control (add/remove services + sensor)
- Add flow control switch + sensor (traffic control, per-WAN bandwidth)
- Add public WAN IP sensor; serial number in gateway attributes
- Add WireGuard VPN client sensor
- Deep-probe v2 across full EG feature set

## 1.4.0
- Deeper diagnostic probe across documented EG modules

## 1.3.0
- WAN control: primary-WAN select, forced-switch toggle, services
- Auto-title entry from model; MAC/firmware in device registry
- Refresh + deep-probe buttons

## 1.2.0
- VLAN-aware clients via subnet matching (named VLANs)
- WAN uplink sensors + active-WAN from mllb

## 1.1.0
- mllb WAN read; device-tracker IP caching

## 1.0.0
- Device trackers per client; per-VLAN sensors; gateway sensor; options flow

## 0.x
- Initial ARP-based client count, sysinfo, WAN online; auth + dispatch
